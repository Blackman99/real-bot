{
  "name": "@real-bot/daemon",
  "license": "MIT",
  "private": true,
  "version": "0.1.0-rc.2",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/main.ts",
    "start": "bun src/main.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "eval:tool-selection": "bun scripts/tool-selection-eval.ts",
    "compile": "bun build --compile src/main.ts --outfile dist/real-bot-daemon"
  },
  "dependencies": {
    "@real-bot/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7.0.2"
  }
}
